#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: lenth
 */
int _strlen(char *s)
{
	int len = 0;

	while (*S != '\0')
	{
		len++;
		S++;
	}

	return (len);
}
